{
  "name": "acornops-management-console",
  "private": true,
  "version": "0.0.1-experimental.1",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "npm run ui:build",
    "predev": "npm run ui:build",
    "dev": "vite",
    "prebuild": "npm run ui:build",
    "build": "vite build",
    "build:analyze": "ANALYZE=true vite build",
    "predesign:catalog": "npm run ui:build",
    "design:catalog": "vite --host 127.0.0.1 --open /design-system.html",
    "design:check": "node scripts/check-design-system.mjs",
    "design:snapshots": "playwright test",
    "design:snapshots:update": "playwright test --update-snapshots",
    "preview": "vite preview",
    "lint": "npm run ui:typecheck && tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "smoke:routes": "node scripts/smoke-routes.mjs",
    "smoke:fixtures": "playwright test --config=playwright.fixtures.config.ts --retries=0 --repeat-each=3",
    "smoke:mcp-parity": "playwright test --config=playwright.mcp-parity.config.ts --retries=0 --repeat-each=3",
    "smoke:nginx": "node scripts/smoke-nginx-headers.mjs",
    "membership:check": "node scripts/check-workspace-membership.mjs",
    "contracts:check": "node scripts/check-contracts.mjs",
    "harness:check": "node scripts/check-harness.mjs",
    "ui:build": "npm run build -w @acornops/ui",
    "ui:typecheck": "npm run typecheck -w @acornops/ui",
    "ui:exports": "node scripts/check-ui-package.mjs",
    "ui:pack": "npm run pack:check -w @acornops/ui",
    "changeset:check": "node scripts/check-ui-changeset.mjs",
    "ui:check": "npm run changeset:check && npm run ui:typecheck && npm run ui:build && npm run ui:exports && npm run ui:pack",
    "changeset": "changeset",
    "version:packages": "changeset version",
    "release:packages": "changeset publish",
    "validate": "npm run ui:check && npm run design:check && npm run lint && npm run test && npm run design:snapshots && npm run smoke:fixtures && npm run smoke:mcp-parity && npm run membership:check && npm run contracts:check && npm run harness:check && npm run build && npm run smoke:routes",
    "validate:ci": "npm run ui:check && npm run design:check && npm run lint && npm run test:coverage && npm run design:snapshots && npm run smoke:fixtures && npm run smoke:mcp-parity && npm run membership:check && npm run contracts:check && npm run harness:check && npm run build && npm run smoke:routes"
  },
  "dependencies": {
    "@acornops/ui": "workspace:*",
    "@fontsource/outfit": "^5.2.8",
    "@fontsource/ubuntu-mono": "^5.2.8",
    "clsx": "^2.1.1",
    "framer-motion": "^12.34.3",
    "i18next": "^26.0.10",
    "lucide-react": "^0.575.0",
    "msw": "^2.15.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-i18next": "^17.0.7",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1",
    "tailwind-merge": "^3.5.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.14.0",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.0.0",
    "@vitest/coverage-v8": "^4.1.5",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "rollup-plugin-visualizer": "^7.0.1",
    "tailwindcss": "^3.4.17",
    "typescript": "~5.8.2",
    "vite": "^6.4.3",
    "vitest": "^4.1.5"
  }
}
