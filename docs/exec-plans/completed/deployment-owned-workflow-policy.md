# Deployment-owned workflow policy

Remove hidden workflow policy literals from manual workflow requests, require the authoritative workflow-options catalog before creation, keep AI settings drafts nullable until server data arrives, and resolve an omitted production API base to same-origin. Validate focused UI/API tests, build checks, and mirrored contracts.

Coordinated by the parent workspace `plan.md`. Related repositories: `control-plane`, `acornops-deployment`, and `docs-website`.

Completed: manual workflow requests omit server policy, catalog failures block create and launch, AI drafts hydrate from server data, production defaults to same-origin, and focused tests, contracts, lint, and build pass.
