---
"@acornops/ui": minor
---

Allow routes to tune page-header description measure, add a wide master-detail
library option and a configurable split-view breakpoint for denser rows, let
compact dense discovery toolbars settle at the `xl` breakpoint, and simplify
workflow creation to one direct action without the manual template picker.
