---
"@acornops/ui": patch
---

Make secondary and icon-only buttons quieter with a transparent, shadowless
outline treatment, add a shared quiet `dangerIcon` variant, and publish the
`control-secondary-boundary` semantic token.
